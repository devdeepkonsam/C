#include <stdio.h>
#include <string.h>

int main(){
    char str[100];
    int i;
    printf("Enter a string: ");
    scanf("%s",string);
    for(i=0:str[i]!='\0':i++){
    }
    printf("size of the string is %d",i);
    
    return 0;
}